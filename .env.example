# Optional overrides — ngrok token and domain are baked into the Docker image.
# Only copy to .env if you need different values locally.

NGROK_AUTHTOKEN=your-ngrok-authtoken
NGROK_DOMAIN=your-name.ngrok-free.dev
